name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    uses: consenlabs/tokenlon-contracts-lib-js/.github/workflows/build.yaml@570a419a43822625690ec9dd6353272f802f625d
    secrets: inherit
